# Prism Backend Configuration

# Search provider API key (optional — falls back to mock if not set)
TAVILY_API_KEY=

# Pipeline mode: "mock" or "real"
PRISM_PIPELINE_MODE=mock

# Server configuration
HOST=0.0.0.0
PORT=8000
