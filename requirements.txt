# Prism — Person A (Pipeline & Deployment)
# Python 3.9+ compatible.

anthropic>=0.40.0        # LLM provider (Claude). Swap freely — see prism/llm.py
tavily-python>=0.5.0     # search() primary provider (§5)
httpx>=0.27.0            # fetch() plain HTTP GET
trafilatura>=1.9.0       # fetch() article-text extraction (§5)
pydantic>=2.6.0          # the A<->D schema contract (schemas.py)
python-dotenv>=1.0.0     # load .env in local dev
